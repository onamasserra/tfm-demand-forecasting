% chapter3.tex — Design and Implementation
% This file is included via \input{chapter3} in TFM_onamas.tex

This chapter presents the complete design and implementation of the demand forecasting system, following the CRISP-DM methodology introduced in Chapter~1. The work progresses from data acquisition and exploration through preprocessing, feature engineering, model development, evaluation, and finally the economic impact analysis that connects forecast accuracy to inventory cost savings.

\section{Data Origin}

\subsection{Data Acquisition}

The dataset used in this project was provided by the thesis supervisor and corresponds to real historical data from a company operating in the automotive sector. The data covers daily demand records for a portfolio of products, along with supplementary information on calendar events, promotional campaigns, and stock levels. The main data was received in a Microsoft Excel file (\texttt{Datos.xlsx}) containing four sheets, each representing a distinct dimension of the business context. Two additional files were provided for the economic impact analysis: \texttt{DatosCicloAprovisionamiento.xlsx} (replenishment cycle and lead time per product) and \texttt{DatosPrecioMedio.xlsx} (average unit price per product).

No personally identifiable information is present in the dataset, and all product references are anonymised using numerical identifiers, ensuring compliance with data privacy requirements as discussed in Section~\ref{s:etic}.

\subsection{Data Description}

The dataset comprises four tables, summarised in Table~\ref{tab:data_sheets}:

\begin{table}[H]
\centering
\small
\caption{Structure of the source dataset (\texttt{Datos.xlsx}).}
\label{tab:data_sheets}
\begin{tabular}{|l|l|r|p{5.5cm}|}
\hline
\textbf{Sheet} & \textbf{Key columns} & \textbf{Rows} & \textbf{Description} \\
\hline
Ventas (Sales) & producto, fecha, udsVenta & 629,391 & Daily unit sales per product \\
\hline
Calendario & fecha, bolOpen, bolHoliday & 787 & Calendar flags: business day and holiday indicators \\
\hline
Promociones & producto, fechaInicio, fechaFin & 26,511 & Promotional campaign periods per product \\
\hline
Stock & producto, fecha, udsStock & 629,391 & Daily stock levels per product \\
\hline
\end{tabular}
\end{table}

The sales sheet constitutes the core of the dataset, containing 629,391 daily observations across 861 unique products over a period of 731 days, spanning from March 2024 to March 2026. Each record represents the number of units sold for a specific product on a specific date.

The calendar sheet provides 787 daily records with two binary indicators: \texttt{bolOpen} (whether the business was open) and \texttt{bolHoliday} (whether the date was a public holiday). The promotions sheet contains 26,511 records defining promotional campaign windows through start and end dates for each product. The stock sheet mirrors the sales structure, recording the closing stock level for each product-date combination.

\subsection{Data Loading}

All four sheets were loaded into memory using the \texttt{pandas} library with the \texttt{openpyxl} engine. The sales and stock tables were merged on the shared keys \texttt{(producto, fecha)}, and the calendar information was joined on \texttt{fecha}. Promotional periods were transformed from date ranges into a binary indicator (\texttt{en\_promo}) at the product-date level, marking each observation as promotional or non-promotional based on whether the date fell within any active campaign for that product.

After merging, the resulting dataset contained 629,391 rows and served as the foundation for all subsequent analysis. The merged dataset was persisted in Apache Parquet format for efficient reloading in downstream scripts.


\clearpage
\section{Data Exploration}

\subsection{Data Summary}

An initial statistical summary of the target variable (\texttt{udsVenta}, units sold) revealed a highly skewed distribution. The mean daily sales per product were 1.19 units, with a median of zero and a standard deviation of 2.22. The maximum observed value was 397 units. This distribution reflects the nature of automotive parts demand, where the majority of product-day combinations register zero sales, punctuated by occasional larger orders.

Table~\ref{tab:sales_stats} presents the key descriptive statistics:

\begin{table}[H]
\centering
\small
\caption{Descriptive statistics of daily unit sales (\texttt{udsVenta}).}
\label{tab:sales_stats}
\begin{tabular}{|l|r|}
\hline
\textbf{Statistic} & \textbf{Value} \\
\hline
Count & 629,391 \\
Mean & 1.19 \\
Std. deviation & 2.22 \\
Median (50\%) & 0.00 \\
75th percentile & 2.00 \\
95th percentile & 5.00 \\
Maximum & 397 \\
Zero-sales proportion & 65.3\% \\
\hline
\end{tabular}
\end{table}

The high proportion of zero-sales observations (65.3\%) is a defining characteristic of this dataset and has important implications for model selection and evaluation, as discussed in subsequent sections.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/01_sales_distribution.png}
\caption{Distribution of daily unit sales (\texttt{udsVenta}). The histogram confirms the extreme right skew, with the majority of observations at zero.}
\label{fig:sales_distribution}
\end{figure}

\subsection{Statistical Analysis}

The exploratory analysis examined demand patterns across several dimensions:

\textbf{Day-of-week patterns.} Sales exhibit a clear weekly cycle, with consistent volumes from Monday through Saturday (mean daily sales between 1.25 and 1.49 units per product). Sunday demand drops sharply to a mean of 0.11 units, with 96.1\% of Sunday observations recording zero sales. This weekly seasonality motivates the inclusion of day-of-week features and the use of a lag-7 naive baseline (same weekday, previous week) as the primary benchmark.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/02_sales_by_day_of_week.png}
\caption{Average daily sales by day of the week. Demand is consistent Monday through Saturday, with a sharp drop on Sundays.}
\label{fig:sales_by_dow}
\end{figure}

\textbf{Monthly and seasonal patterns.} Monthly aggregated sales show moderate variation across the year, without a single dominant seasonal peak. However, certain months consistently show higher activity, suggesting that calendar-based features (month, week of year) may contribute predictive value.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/09_monthly_by_year.png}
\caption{Average daily sales by month, broken down by year. Monthly variation is moderate, with no single dominant seasonal peak.}
\label{fig:monthly_by_year}
\end{figure}

\textbf{Promotional effects.} Comparing sales during promotional and non-promotional periods revealed that mean sales during promotions (1.36 units) were slightly higher than during non-promotional periods (1.16 units). However, the difference is modest, and the promotional flag alone does not capture the full complexity of promotional demand dynamics, which motivated the engineering of additional promotional features described in Section~3.4.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/05_promo_vs_nonpromo.png}
\caption{Comparison of sales distributions during promotional and non-promotional periods. Mean sales during promotions (1.36 units) are slightly higher than non-promotional periods (1.16 units).}
\label{fig:promo_vs_nonpromo}
\end{figure}

\textbf{Autocorrelation structure.} Autocorrelation analysis on a sample of products confirmed significant positive autocorrelation at lags 1, 7, 14, and 28, with the strongest signal at lag 7 (weekly periodicity). This finding directly supports the creation of lag features at these intervals and validates the choice of lag-7 as the naive baseline.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{figures/08_autocorrelation_sample.png}
\caption{Autocorrelation function (ACF) for a sample of products, showing significant positive autocorrelation at weekly intervals.}
\label{fig:autocorrelation}
\end{figure}

\textbf{Seasonal decomposition.} A classical additive decomposition of the aggregated daily sales series was performed with a weekly period ($p=7$), separating the observed signal into trend, seasonal, and residual components. The trend component reveals a stable demand level across the two-year period, while the seasonal component confirms the strong weekly cycle identified in the autocorrelation analysis. The residual component shows no systematic patterns, suggesting that the weekly periodicity captures the dominant temporal structure in the data.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{figures/10b_seasonal_decomposition.png}
\caption{Additive seasonal decomposition of aggregated daily sales (period = 7 days). The weekly seasonal component is clearly visible, with a stable trend and no systematic residual patterns.}
\label{fig:seasonal_decomposition}
\end{figure}

\subsection{Data Quality Analysis}

The data quality assessment identified the following issues:

\textbf{Missing values.} No explicit missing values were found in any of the four source tables. However, implicit gaps exist where lag features cannot be computed for the earliest dates in each product's history (addressed during feature engineering).

\textbf{Negative values.} No negative sales or stock values were present in this dataset.

\textbf{Stock breaks.} A stock break is defined as a day where both sales and stock are zero, potentially indicating that demand existed but could not be fulfilled due to inventory unavailability. Approximately 2.0\% of observations met this criterion. These were treated during data cleaning by imputing the product's historical mean sales for the corresponding weekday.

\textbf{Outliers.} Outlier detection was performed using a 3$\sigma$ threshold per product per month. This identified approximately 8,895 observations (1.41\% of the dataset) as outliers, which were capped at the upper threshold value. The per-product-per-month approach was preferred over a global threshold to account for the varying demand scales and seasonal patterns across products.


\clearpage
\section{Data Preparation}

\subsection{Data Cleaning}

The data cleaning process addressed the two quality issues identified during exploration:

\begin{enumerate}
    \item \textbf{Stock break imputation:} For observations where both \texttt{udsVenta} and \texttt{udsStock} were zero, the sales value was replaced with the mean sales for that product on the same weekday, computed over the preceding 30 days. This approach assumes that zero sales on a day with zero stock reflects a supply-side constraint rather than genuine zero demand, and uses a localised historical average to provide a reasonable demand estimate.

    \item \textbf{Outlier capping:} Values exceeding 3 standard deviations above the mean for each product-month combination were capped at the threshold value. This preserves the general distribution shape while limiting the influence of extreme observations on model training. A total of 8,895 values (1.41\% of the dataset) were capped.
\end{enumerate}

After cleaning, the dataset retained its original 629,391 rows with no records removed, ensuring that the temporal continuity required for lag feature computation was preserved.

\subsection{Data Transformation}

The date column (\texttt{fecha}) was parsed into a \texttt{datetime} type, and the dataset was sorted by product and date to ensure correct temporal ordering for all subsequent operations. The promotional periods from the source data were transformed from date-range format into a daily binary indicator (\texttt{en\_promo}), assigned at the product-date level.

No logarithmic or power transformations were applied to the target variable. While such transformations can stabilise variance in skewed distributions, the tree-based models used in this work (Random Forest, XGBoost, LightGBM) are inherently robust to non-normal distributions and do not require target transformation for effective learning.

\subsection{Data Scaling}

Feature scaling (standardisation or normalisation) was not applied for the tree-based models, as decision tree ensembles are invariant to monotonic transformations of input features. The split-based learning mechanism of these algorithms operates on feature rankings rather than absolute values, making scaling unnecessary.

For the LSTM model, min-max normalisation was applied to all input features to facilitate gradient-based optimisation. Each feature was scaled to the $[0, 1]$ range using the training set statistics, and the same transformation was applied to the validation and test sets to prevent information leakage. Predictions were inverse-transformed back to the original scale before computing evaluation metrics.

\clearpage
\section{Feature Engineering}

Feature engineering is a central component of this work. A comprehensive set of 36 engineered features was designed to capture temporal dependencies, demand dynamics, and promotional effects. The features are organised into five categories, summarised in Table~\ref{tab:features}.

\begin{table}[H]
\centering
\small
\caption{Engineered features by category.}
\label{tab:features}
\begin{tabular}{|l|p{7cm}|r|}
\hline
\textbf{Category} & \textbf{Features} & \textbf{Count} \\
\hline
Original variables & udsStock, bolOpen, bolHoliday, en\_promo & 4 \\
\hline
Calendar features & year, month, day\_of\_week, week\_of\_year, week\_of\_month, is\_weekend, is\_month\_start, is\_month\_end & 8 \\
\hline
Lag features & lag\_1, lag\_7, lag\_14, lag\_28 & 4 \\
\hline
Rolling statistics & roll\_mean/std/min/max at 7, 14, and 28-day windows; ewma\_7, ewma\_28 & 14 \\
\hline
Promotional features & days\_since\_promo\_end, post\_promo\_7d, promo\_x\_lag7 & 3 \\
\hline
Product-level statistics & prod\_mean\_sales, prod\_std\_sales, prod\_median\_sales & 3 \\
\hline
\multicolumn{2}{|r|}{\textbf{Total}} & \textbf{36} \\
\hline
\end{tabular}
\end{table}

\textbf{Lag features} capture the autoregressive structure identified during exploration. The lag-7 feature (same weekday, previous week) serves dual purpose: it is used directly as the naive baseline forecast and as an input feature for the machine learning models. Lags at 1, 14, and 28 days capture short-term momentum, biweekly patterns, and monthly cycles respectively.

\textbf{Rolling statistics} provide smoothed representations of recent demand behaviour. For each window size (7, 14, and 28 days), the mean, standard deviation, minimum, and maximum of past sales are computed. Additionally, exponentially weighted moving averages (EWMA) with spans of 7 and 28 days provide adaptive smoothing that gives greater weight to more recent observations.

\textbf{Promotional features} go beyond the binary indicator to capture the temporal dynamics of promotions. The \texttt{days\_since\_promo\_end} feature tracks the recency of the last promotional period, \texttt{post\_promo\_7d} flags the week immediately following a promotion (to capture post-promotional demand depression), and \texttt{promo\_x\_lag7} is an interaction term between the promotional flag and the lag-7 sales value.

All lag and rolling features were computed strictly using past data to prevent information leakage. Observations where lag features could not be computed (the first 28 days of each product's history) were dropped, reducing the dataset from 629,391 to 605,283 rows. The final feature matrix was saved in Parquet format for use in the modelling phase.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{figures/10_feature_correlations.png}
\caption{Pearson correlation of each engineered feature with the target variable (\texttt{udsVenta}). The 28-day EWMA and rolling mean features show the strongest positive correlation with sales.}
\label{fig:feature_correlations}
\end{figure}


\clearpage
\section{Modelling}

\subsection{Model Selection}

Six forecasting approaches were implemented, spanning a naive baseline, a classical time series method (ARIMA), three tree-based ensemble models, and a deep learning architecture (LSTM):

\begin{enumerate}
    \item \textbf{Naive baseline (lag-7):} The simplest possible forecast, predicting that demand on any given day equals the demand observed on the same weekday of the previous week. This baseline exploits the strong weekly autocorrelation identified during exploration and provides a meaningful benchmark against which machine learning models must demonstrate improvement.

    \item \textbf{ARIMA (Seasonal):} A classical time series approach fitted independently per product using the \texttt{auto\_arima} implementation from the \texttt{pmdarima} library \citep{smith2017pmdarima}. Each product's demand series was modelled with seasonal ARIMA (SARIMA) with weekly periodicity ($m=7$), automatically selecting the optimal $(p,d,q)(P,D,Q)_7$ orders via stepwise search. The ``integrated'' component (the $d$ parameter) handles non-stationarity by differencing the series, transforming raw demand values into changes between consecutive observations. This makes the statistical properties stable over time, which is a requirement for the autoregressive and moving average components to work correctly. Unlike the other models, ARIMA operates on the raw sales series without engineered features and fits 861 separate models (one per product). This per-product approach captures local demand patterns effectively but cannot leverage cross-product learning, as each model only sees its own product's history.

    \item \textbf{Random Forest:} An ensemble method that combines hundreds of independent decision trees to produce a single prediction \citep{pedregosa2011sklearn}. Each individual decision tree works by recursively partitioning the feature space through a series of binary splits. At each internal node, the tree selects the feature and threshold that best separates the data (e.g., ``is the 28-day EWMA greater than 2.5 units?''), directing observations left or right until they reach a leaf node containing the predicted value. A single tree is prone to overfitting, memorising noise in the training data rather than learning generalisable patterns. Random Forest addresses this by training each tree on a different random bootstrap sample of the data and considering only a random subset of features at each split. The final prediction is the average of all individual tree predictions. Because each tree sees different data and features, their individual errors tend to be uncorrelated and cancel out when averaged, producing a more stable and accurate ensemble prediction.

    \item \textbf{XGBoost:} A gradient boosting framework that builds decision trees sequentially rather than in parallel \citep{chen2016xgboost}. The key idea is that each new tree is trained not on the original target variable, but on the residual errors of the previous trees. The first tree makes a rough prediction, the second tree learns to predict where the first tree went wrong, the third tree corrects the remaining errors, and so on. The final prediction is the weighted sum of all trees' contributions. This sequential error-correction approach allows gradient boosting to achieve high accuracy with relatively shallow trees (typically 4--8 levels deep), as each tree only needs to capture a small improvement. XGBoost incorporates L1 and L2 regularisation to prevent overfitting, column subsampling (using a random subset of features per tree, similar to Random Forest), and efficient handling of sparse data through a default direction for missing values at each split. This last property makes it well-suited to the high proportion of zero-demand observations in this dataset.

    \item \textbf{LightGBM:} A gradient boosting implementation that follows the same sequential error-correction strategy as XGBoost but with key differences in how trees are constructed \citep{ke2017lightgbm}. While XGBoost grows trees level by level (expanding all nodes at the same depth before moving deeper), LightGBM grows trees leaf by leaf, always expanding the leaf with the largest potential error reduction. This leaf-wise strategy produces deeper, more asymmetric trees that can capture complex patterns with fewer total nodes. LightGBM also uses histogram-based binning, which groups continuous feature values into discrete bins before searching for the optimal split point. This reduces the computational cost from $O(n)$ to $O(k)$ per split (where $k$ is the number of bins, typically 255), enabling significantly faster training on large datasets. LightGBM was among the dominant algorithms in the M5 forecasting competition \citep{makridakis2021m5findings}, one of the largest demand forecasting benchmarks to date.

    \item \textbf{LSTM (Long Short-Term Memory):} A recurrent neural network architecture designed to capture temporal dependencies in sequential data \citep{hochreiter1997lstm}. Unlike tree-based methods, which treat each observation as an independent row of features, LSTM processes input data as ordered sequences, maintaining an internal memory state that evolves as it reads through the time steps. The model receives 14 consecutive days of 33 features as input. At each time step, the LSTM cell updates its internal state through three learned gates: the \textit{forget gate} decides what information from previous days to discard (e.g., an old promotional effect that is no longer relevant), the \textit{input gate} decides what new information to store (e.g., a sudden demand spike), and the \textit{output gate} decides what to expose as the hidden state for the next step. This gating mechanism is what distinguishes LSTMs from simple recurrent networks, which suffer from vanishing gradients when trying to learn dependencies across many time steps. The model was implemented in PyTorch \citep{paszke2019pytorch} with a single LSTM layer of 32 hidden units. Only the output of the final time step (Day 14) is fed to a fully connected linear layer to produce the demand forecast for the next day. Training used the Adam optimiser (learning rate 0.003, batch size 512) with early stopping (patience of 10 epochs) on all 861 products. Training was monitored on a held-out validation set and stopped automatically when the validation loss ceased to improve, which occurred at epoch 15 out of a maximum of 100. All input features were scaled to the $[0, 1]$ range using min-max normalisation fitted on the training set, as neural networks are sensitive to feature magnitudes.
\end{enumerate}

All tree-based models were trained as a single global model on the full set of 33 input features across all 861 products. XGBoost and LightGBM used early stopping (patience of 20 rounds) to automatically determine the optimal number of boosting iterations, preventing overfitting while allowing the models to train for as many rounds as needed. The LSTM was also trained globally with early stopping (patience of 10 epochs). This global approach allows cross-product learning, where patterns discovered in one product's data help predict similar products. ARIMA, by contrast, fits 861 independent per-product models on the raw sales series without engineered features. All predictions were clipped to a minimum of zero to prevent negative forecast values.

\subsection{Definition of Accuracy Indicators}

Four complementary metrics were used to evaluate forecast performance, as introduced in Chapter~2:

\begin{itemize}
    \item \textbf{MAE} (Mean Absolute Error): average absolute deviation between forecast and actual demand, in the same units as the target variable.
    \item \textbf{RMSE} (Root Mean Squared Error): penalises larger errors more heavily than MAE, providing a measure sensitive to occasional large deviations.
    \item \textbf{MAPE} (Mean Absolute Percentage Error): expresses errors as a percentage of actual demand, computed only for observations where actual demand is greater than zero to avoid division by zero.
    \item \textbf{$\sigma_e$} (forecast error standard deviation): the standard deviation of the residuals ($D_t - F_t$), which directly enters the safety stock calculation (Equation~\ref{eq:safety_stock}) and thus provides the link between forecast accuracy and inventory cost.
\end{itemize}

\subsection{Definition of Training and Validation Periods}

Models were evaluated using a rolling forecast origin strategy, where the training window expands forward and the model is tested on the next unseen period \citep{hyndman2021forecasting}.

Three validation folds were defined, each with a 30-day test window, working backwards from the most recent date in the dataset:

\begin{table}[H]
\centering
\small
\caption{Rolling forecast origin validation folds.}
\label{tab:folds}
\begin{tabular}{|c|c|c|c|}
\hline
\textbf{Fold} & \textbf{Training period end} & \textbf{Test period} & \textbf{Test days} \\
\hline
1 & 2025-12-22 & 2025-12-23 -- 2026-01-21 & 30 \\
2 & 2026-01-21 & 2026-01-22 -- 2026-02-20 & 30 \\
3 & 2026-02-20 & 2026-02-21 -- 2026-03-22 & 30 \\
\hline
\end{tabular}
\end{table}

Figure~\ref{fig:rolling_validation} illustrates this expanding window strategy visually.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    train/.style={fill=blue!20, draw=blue!50, minimum height=0.7cm},
    test/.style={fill=blue!60, draw=blue!70, minimum height=0.7cm},
    unused/.style={fill=gray!10, draw=gray!30, minimum height=0.7cm},
    arrow/.style={->, >=stealth, thick},
    note/.style={font=\footnotesize, text=gray!50!black}
]

% Timeline
\def\totalwidth{12}
\def\rowsep{1.3}

% Date markers
\node[note] at (0, 3*\rowsep + 0.8) {Apr 2024};
\node[note] at (8.57, 3*\rowsep + 0.8) {Dec 2025};
\node[note] at (\totalwidth, 3*\rowsep + 0.8) {Mar 2026};

% Fold 1
\node[note, left] at (-0.1, 2*\rowsep) {Fold 1};
\node[train, minimum width=8.57cm, anchor=west] at (0, 2*\rowsep) {};
\node[test, minimum width=1.14cm, anchor=west] at (8.57, 2*\rowsep) {};
\node[unused, minimum width=2.29cm, anchor=west] at (9.71, 2*\rowsep) {};
\node[note] at (4.28, 2*\rowsep) {Train (641 days)};
\node[note, white] at (9.14, 2*\rowsep) {\scriptsize 30d};

% Fold 2
\node[note, left] at (-0.1, 1*\rowsep) {Fold 2};
\node[train, minimum width=9.71cm, anchor=west] at (0, 1*\rowsep) {};
\node[test, minimum width=1.14cm, anchor=west] at (9.71, 1*\rowsep) {};
\node[unused, minimum width=1.15cm, anchor=west] at (10.85, 1*\rowsep) {};
\node[note] at (4.85, 1*\rowsep) {Train (671 days)};
\node[note, white] at (10.28, 1*\rowsep) {\scriptsize 30d};

% Fold 3
\node[note, left] at (-0.1, 0) {Fold 3};
\node[train, minimum width=10.85cm, anchor=west] at (0, 0) {};
\node[test, minimum width=1.15cm, anchor=west] at (10.85, 0) {};
\node[note] at (5.42, 0) {Train (701 days)};
\node[note, white] at (11.42, 0) {\scriptsize 30d};

% Legend
\node[train, minimum width=0.6cm] at (3, -1) {};
\node[note, right] at (3.35, -1) {Training data};
\node[test, minimum width=0.6cm] at (6.5, -1) {};
\node[note, right] at (6.85, -1) {Validation (30 days)};
\node[unused, minimum width=0.6cm] at (10, -1) {};
\node[note, right] at (10.35, -1) {Not used};

\end{tikzpicture}
\caption{Rolling forecast origin validation strategy. The training window expands with each fold while the 30-day validation window slides forward in time. This ensures that models are never trained on future data.}
\label{fig:rolling_validation}
\end{figure}

For each fold, the training set includes all data up to the fold's cutoff date (expanding window), and the model is evaluated on the subsequent 30-day period. Final reported metrics are averaged across all three folds.

\subsection{Hyperparameter Tuning}

Hyperparameters for the three tree-based models were optimised using Optuna \citep{akiba2019optuna}, a Bayesian optimisation framework that efficiently explores the parameter space using a Tree-structured Parzen Estimator (TPE) sampler.

To balance computational cost with tuning quality, the optimisation was conducted on a representative subset of 50 randomly sampled products using the first validation fold. Each model was allocated 30 Optuna trials, with RMSE on the validation set as the objective function. The optimised hyperparameters were then applied to the full dataset across all validation folds.

Table~\ref{tab:tuned_params} presents the key tuned hyperparameters for each model:

\begin{table}[H]
\centering
\small
\caption{Optimised hyperparameters via Optuna (30 trials per model).}
\label{tab:tuned_params}
\begin{tabular}{|l|l|r|}
\hline
\textbf{Model} & \textbf{Parameter} & \textbf{Value} \\
\hline
\multirow{5}{*}{LightGBM} & n\_estimators & 100 \\
 & learning\_rate & 0.030 \\
 & num\_leaves & 16 \\
 & max\_depth & 8 \\
 & min\_child\_samples & 41 \\
\hline
\multirow{5}{*}{XGBoost} & n\_estimators & 200 \\
 & learning\_rate & 0.032 \\
 & max\_depth & 4 \\
 & min\_child\_weight & 15 \\
 & subsample & 0.82 \\
\hline
\multirow{4}{*}{Random Forest} & n\_estimators & 250 \\
 & max\_depth & 8 \\
 & min\_samples\_leaf & 17 \\
 & max\_features & 0.49 \\
\hline
\end{tabular}
\end{table}

The relatively shallow tree depths (4--8) and moderate learning rates (0.030--0.032) selected by the optimiser suggest that the models benefit from regularisation, which is consistent with the high proportion of zero-demand observations in the dataset.


\clearpage
\section{Evaluation}

\subsection{Accuracy Results Obtained}

Table~\ref{tab:results_summary} presents the average performance of each model across the three validation folds:

\begin{table}[H]
\centering
\small
\caption{Forecast accuracy on the last validation fold (30 days, 861 products).}
\label{tab:results_summary}
\begin{tabular}{|l|r|r|r|r|}
\hline
\textbf{Model} & \textbf{MAE} & \textbf{RMSE} & \textbf{MAPE (\%)} & \textbf{$\sigma_e$} \\
\hline
LightGBM & 1.092 & 1.641 & 66.9 & 1.641 \\
XGBoost & 1.090 & 1.634 & 67.1 & 1.634 \\
Random Forest & 1.102 & 1.635 & 68.9 & 1.635 \\
LSTM & 1.089 & 1.643 & 66.5 & 1.643 \\
ARIMA & 1.211 & 1.734 & 77.3 & 1.732 \\
Naive (lag-7) & 1.256 & 2.277 & 72.9 & 2.277 \\
\hline
\end{tabular}
\end{table}

The three tree-based models and LSTM achieve virtually identical performance, with XGBoost holding a marginal edge on RMSE (1.634) and LSTM achieving the lowest MAE (1.089). All four models reduce RMSE by approximately 28\% compared to the naive baseline (2.277). ARIMA (1.734) also outperforms the baseline substantially, though with a gap behind the other ML models. The near-identical performance of the tree models and LSTM suggests that the feature engineering, rather than the specific algorithm, is the primary driver of forecast quality.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/24_all_models_comparison.png}
\caption{Comparison of forecast accuracy (RMSE) across all six models. The tree-based models and LSTM form a tight cluster with virtually identical performance, all substantially outperforming the naive baseline.}
\label{fig:all_models_comparison}
\end{figure}

This ranking is consistent with the findings of the M5 forecasting competition \citep{makridakis2021m5findings}, where gradient boosting algorithms consistently outperformed both classical statistical methods and deep learning approaches.

\subsection{Comparison of Results Across Models}

\textbf{Promotional vs. non-promotional performance.} Table~\ref{tab:promo_results} compares model performance across demand segments:

\begin{table}[H]
\centering
\small
\caption{Model performance by demand segment (last validation fold).}
\label{tab:promo_results}
\begin{tabular}{|l|l|r|r|r|r|}
\hline
\textbf{Segment} & \textbf{Model} & \textbf{MAE} & \textbf{RMSE} & \textbf{MAPE (\%)} & \textbf{$\sigma_e$} \\
\hline
\multirow{6}{*}{Non-Promo} & XGBoost & 1.070 & 1.640 & 65.2 & 1.640 \\
 & LightGBM & 1.066 & 1.644 & 64.3 & 1.644 \\
 & Random Forest & 1.088 & 1.642 & 68.0 & 1.641 \\
 & LSTM & 1.076 & 1.649 & 66.0 & 1.649 \\
 & ARIMA & 1.195 & 1.732 & 76.4 & 1.729 \\
 & Naive (lag-7) & 1.237 & 2.288 & 71.5 & 2.288 \\
\hline
\multirow{6}{*}{Promo} & XGBoost & 1.164 & 1.613 & 73.9 & 1.613 \\
 & LightGBM & 1.184 & 1.631 & 76.3 & 1.629 \\
 & Random Forest & 1.151 & 1.610 & 72.1 & 1.610 \\
 & LSTM & 1.135 & 1.622 & 68.6 & 1.621 \\
 & ARIMA & 1.271 & 1.740 & 80.9 & 1.739 \\
 & Naive (lag-7) & 1.326 & 2.233 & 77.9 & 2.233 \\
\hline
\end{tabular}
\end{table}

Promotional demand is consistently harder to predict across all models, though the difference is less pronounced in this dataset. All five ML models maintain their advantage over the naive baseline in both segments. The tree-based models achieve RMSE improvements of approximately 28\%, while LSTM and ARIMA also outperform the naive baseline by meaningful margins.

\textbf{Per-product RMSE distribution.} Analysis at the individual product level reveals that the tree-based models achieve a median per-product RMSE of approximately 1.32, compared to 1.83 for the naive baseline. The improvement is not uniform: some products see RMSE reductions exceeding 8 units, while approximately 35 products show marginally worse performance with ML than with the naive approach. These products tend to have very low and stable demand where the simple lag-7 heuristic is already near-optimal.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{figures/17_error_distributions.png}
\caption{Distribution of forecast errors for all six models. The tree-based models produce tighter, more symmetric error distributions compared to the naive baseline, with ARIMA and LSTM falling in between.}
\label{fig:error_distributions}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{figures/18_per_product_rmse_dist.png}
\caption{Distribution of per-product RMSE values: Naive vs each ML model. All five models shift the distribution towards lower RMSE values, with the three tree-based models showing the largest improvement.}
\label{fig:per_product_rmse}
\end{figure}

\textbf{SHAP feature importance.} To understand the drivers of model predictions, SHAP (SHapley Additive exPlanations) analysis \citep{lundberg2017shap} was performed on the LightGBM model. Table~\ref{tab:shap_top10} presents the ten most influential features:

\begin{table}[H]
\centering
\small
\caption{Top 10 features by mean absolute SHAP value (LightGBM).}
\label{tab:shap_top10}
\begin{tabular}{|r|l|r|}
\hline
\textbf{Rank} & \textbf{Feature} & \textbf{Mean $|$SHAP$|$} \\
\hline
1 & ewma\_28 & 0.473 \\
2 & day\_of\_week & 0.260 \\
3 & roll\_mean\_28 & 0.108 \\
4 & udsStock & 0.037 \\
5 & lag\_7 & 0.024 \\
6 & lag\_28 & 0.022 \\
7 & lag\_14 & 0.018 \\
8 & roll\_max\_28 & 0.018 \\
9 & bolOpen & 0.018 \\
10 & bolHoliday & 0.004 \\
\hline
\end{tabular}
\end{table}

The 28-day EWMA emerges as the single most important predictor, followed by the day of week and the 28-day rolling mean. This confirms that the model relies heavily on smoothed representations of recent demand history, with calendar effects providing secondary but significant predictive power. Notably, the longer-horizon features (28-day windows) dominate over shorter-horizon ones (7-day), suggesting that the model captures medium-term demand trends rather than relying solely on short-term momentum.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/15_shap_bar.png}
\caption{SHAP feature importance (mean absolute SHAP values) for LightGBM. The 28-day EWMA dominates, followed by calendar and rolling statistics features.}
\label{fig:shap_bar}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/14_shap_summary.png}
\caption{SHAP summary plot showing the direction and magnitude of feature effects. Red indicates high feature values; blue indicates low values. High \texttt{ewma\_28} values push predictions upward, consistent with higher recent demand leading to higher forecasts.}
\label{fig:shap_summary}
\end{figure}

\subsection{Conclusions from Results}

The evaluation yields several key findings:

\begin{enumerate}
    \item The three tree-based models and LSTM achieve virtually identical performance, with RMSE values within 1\% of each other (1.634--1.643). This suggests that the feature engineering, rather than the specific algorithm choice, is the primary driver of forecast quality. The LSTM, trained on all 861 products with early stopping, matches the tree models despite using a fundamentally different learning approach.

    \item ARIMA occupies a separate tier. ARIMA (RMSE = 1.734) achieves a 23.9\% improvement over the naive baseline when fitted to all 861 products, demonstrating that classical time series methods remain effective. However, its per-product fitting approach cannot leverage cross-product patterns, which explains the gap with the global models.

    \item The improvement over the naive baseline is substantial across all ML approaches (24--28\% RMSE reduction), demonstrating the value of incorporating engineered features and systematic model training over simple heuristic forecasting.

    \item Promotional demand remains challenging, though the ML models maintain their relative advantage over the baseline in both promotional and non-promotional segments.

    \item SHAP analysis reveals that smoothed demand history (EWMA and rolling statistics) and calendar features are the most influential predictors, validating the feature engineering strategy adopted in this work.

    \item The forecast error standard deviation ($\sigma_e \approx 1.64$ for the top four models, $\sigma_e \approx 2.28$ for the naive baseline) provides the direct input to the safety stock calculation presented in the following section.
\end{enumerate}


\clearpage
\section{Impact of Algorithm Improvement}

\subsection{Impact on Processes}

The transition from a naive forecasting approach to machine learning models has implications beyond numerical accuracy improvements. From an operational perspective, the key process impacts are:

\textbf{Safety stock reduction.} Using the safety stock formulation from Equation~\ref{eq:safety_stock} with a 95\% service level ($z = 1.645$) and per-product protection periods computed as the sum of the replenishment cycle (\texttt{diasEntrePedidos}) and lead time (\texttt{diasLeadtime}) from the company's operational data, the total safety stock across all 861 products decreases from 12,544 units under the naive forecast to 9,090 units under XGBoost, a reduction of 27.5\%. This means that the same service level can be maintained with significantly less inventory investment.

\textbf{Forecast error symmetry.} Analysis of error distributions shows that the ML models produce approximately symmetric errors centred near zero, whereas the naive baseline exhibits a slight positive bias (tendency to overforecast). Symmetric errors are preferable for inventory planning, as they indicate that the forecast is equally likely to overestimate or underestimate demand, allowing safety stock to function as intended.

\textbf{Sensitivity to service level.} Table~\ref{tab:sensitivity} presents the total safety stock requirements across different service levels:

\begin{table}[H]
\centering
\small
\caption{Total safety stock (units) by model and service level.}
\label{tab:sensitivity}
\begin{tabular}{|l|r|r|r|}
\hline
\textbf{Model} & \textbf{90\%} & \textbf{95\%} & \textbf{99\%} \\
\hline
XGBoost & 7,093 & 9,104 & 12,875 \\
Random Forest & 7,091 & 9,102 & 12,872 \\
LightGBM & 7,119 & 9,137 & 12,923 \\
LSTM & 7,139 & 9,163 & 12,959 \\
ARIMA & 7,482 & 9,604 & 13,583 \\
Naive (lag-7) & 9,773 & 12,544 & 17,740 \\
\hline
\end{tabular}
\end{table}

The ML models consistently require approximately 27\% less safety stock than the naive baseline across all service levels, demonstrating that the improvement is robust and not dependent on a specific service level assumption.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/21_safety_stock_by_service_level.png}
\caption{Total safety stock requirements by model and service level. The tree-based models maintain a consistent 27\% advantage over the naive baseline across all service levels tested (90\%, 95\%, 99\%).}
\label{fig:safety_stock_service_level}
\end{figure}

\subsection{Economic Impact}

The economic impact analysis translates the forecast accuracy improvements into monetary terms using the methodology described in the \texttt{EstimacionCostesStock} document provided by the thesis supervisor, combined with real per-product cost parameters from the company.

\textbf{Cost parameters.} Two additional datasets were used for this analysis:
\begin{itemize}
    \item \texttt{DatosCicloAprovisionamiento.xlsx}: provides the replenishment cycle (\texttt{diasEntrePedidos}, mean = 13.8 days) and lead time (\texttt{diasLeadtime}, mean = 5.4 days) for each of the 1,000 products.
    \item \texttt{DatosPrecioMedio.xlsx}: provides the average unit price (\texttt{eurPrecioMedio}, mean = \euro49.75, range \euro0.01 -- \euro625.26) for each product.
\end{itemize}

The protection period for each product was computed as the sum of its replenishment cycle and lead time ($T_i + L_i$), with a mean of 19.1 days across the portfolio.

\textbf{Safety stock cost.} Following the formulation from Equation~\ref{eq:ss_cost}, the daily holding cost for each product's safety stock was computed as:

\[
\text{DailyCost}_i = 5\% \times \text{price}_i \times SS_i
\]

\noindent where $SS_i = z \times RMSE_i \times \sqrt{T_i + L_i}$ and the 5\% rate represents the combined cost of warehousing, insurance, and capital opportunity cost, as specified in the supervisor's methodology document.

Table~\ref{tab:economic_summary} presents the total annual safety stock cost for each model:

\begin{table}[H]
\centering
\small
\caption{Annual safety stock cost by model (95\% service level, real per-product parameters).}
\label{tab:economic_summary}
\begin{tabular}{|l|r|r|r|r|}
\hline
\textbf{Model} & \textbf{Avg RMSE} & \textbf{Total SS (units)} & \textbf{Annual Cost (\euro)} & \textbf{Savings vs Naive} \\
\hline
Naive (lag-7) & 2.01 & 12,544 & 11,398,048 & --- \\
XGBoost & 1.45 & 9,104 & 8,333,650 & \euro3,064,397 (26.9\%) \\
Random Forest & 1.45 & 9,102 & 8,308,914 & \euro3,089,134 (27.1\%) \\
LightGBM & 1.46 & 9,137 & 8,374,044 & \euro3,024,004 (26.5\%) \\
LSTM & 1.46 & 9,163 & 8,398,387 & \euro2,999,661 (26.3\%) \\
ARIMA & 1.53 & 9,604 & 8,837,910 & \euro2,560,138 (22.5\%) \\
\hline
\end{tabular}
\end{table}

All five ML models achieve substantial savings compared to the naive baseline. The three tree-based models and LSTM perform nearly identically, saving approximately 26--27\% (\euro3.0--3.1M annually). ARIMA, despite being the weakest ML model, still delivers a 22.5\% cost reduction. The total cost differences among the top four models are less than \euro90,000.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/20_annual_cost_comparison.png}
\caption{Annual safety stock cost by model. All five ML models reduce total annual costs compared to the naive baseline, with the three tree-based models achieving approximately 27\% savings.}
\label{fig:annual_cost_comparison}
\end{figure}

Figure~\ref{fig:per_product_cost_comparison} provides a per-product view of the cost savings. Each subplot compares the annual safety stock cost of the naive baseline (grey) against one of the five ML models, for the top 200 products ranked by naive cost. The gap between the two curves represents the savings for each product. The three tree-based models show nearly identical savings patterns, while ARIMA and LSTM also achieve consistent reductions, though slightly smaller in magnitude.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{figures/25_per_product_cost_comparison.png}
\caption{Per-product annual safety stock cost for the top 200 products (ranked by naive cost). Grey area: naive baseline. Coloured area: ML model. The gap represents cost savings.}
\label{fig:per_product_cost_comparison}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{figures/22_safety_stock_distribution.png}
\caption{Per-product safety stock distribution: Naive vs each ML model. All five ML models shift the distribution towards lower safety stock requirements across the product portfolio.}
\label{fig:safety_stock_distribution}
\end{figure}

These results demonstrate that the forecast accuracy improvements documented in Section~3.6 translate into substantial economic benefits when applied with real company data. The tree-based models and LSTM achieve approximately 26--27\% cost savings, while ARIMA delivers a 22.5\% reduction, confirming the theoretical relationship between forecast error and inventory costs established in Chapter~2 (Equations~\ref{eq:ss_cost} and \ref{eq:cost_savings}).

It is important to note that the cost parameters used in this analysis (5\% daily holding cost rate, per-product prices and replenishment cycles) were provided by the thesis supervisor and reflect the company's actual operational context. The relative ranking of models and the percentage savings are robust to reasonable variations in the holding cost rate, as the underlying driver is the reduction in forecast error rather than the specific cost assumptions. A sensitivity analysis across service levels (Table~\ref{tab:sensitivity}) confirms that the ML advantage is maintained for all five models regardless of the risk tolerance adopted by the organisation.
